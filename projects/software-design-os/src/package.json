{
  "name": "software-design-os",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @sdos/web dev & pnpm --filter @sdos/api dev",
    "dev:web": "pnpm --filter @sdos/web dev",
    "dev:api": "pnpm --filter @sdos/api dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
